spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jovempaulista
    username: postgres
    password: "0512"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
